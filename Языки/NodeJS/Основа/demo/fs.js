const fs = require('fs'); // позволяет работать с сист файлами 
const path = require('path');
/*
fs.mkdir(path.join(__dirname, 'test'), (err) => {
    if (err) {
        throw err; 
    }

    console.log('Папка создана');
});
*/
